@model BookstoreSistem.Models.Usuario

<div class="container-fluid vh-100 p-5 d-flex flex-column align-items-center justify-content-center">

    <div class="row w-75">
        <div class="col-lg-12 shadow shadow-lg p-5 d-flex flex-column align-items-center">

            <h2 class="mb-4 text-danger">Eliminar Usuario</h2>

            <div class="mb-4 w-100">
                <p><strong>Id:</strong> @Model.Id</p>
                <p><strong>Nombre:</strong> @Model.Nombre</p>
                <p><strong>Apellido:</strong> @Model.Apellido</p>
                <p><strong>Edad:</strong> @Model.Edad</p>
            </div>

            <p class="text-warning">¿Está seguro de que desea eliminar este usuario?</p>

            <form asp-action="Destroy" method="post" class="d-flex flex-column align-items-center">

                <input type="hidden" asp-for="Id" />

                <div class="mt-3 d-flex gap-3">
                    <button type="submit" class="btn btn-danger">Eliminar</button>

                    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                </div>

            </form>

        </div>
    </div>

</div>